apiVersion: 1
datasources:
  - name: Google BigQuery
    type: marcusolsson-bigquery-datasource
    access: proxy
    jsonData:
      authenticationType: jwt
      clientEmail: swarvjagdale@gmail.com
    secureJsonData:
      privateKeyJson: /etc/grafana/servicekey.json
